class EEGCohortModel:
    def __init__(self, task_dto, subjects, data_dir):
        raise NotImplementedError(
            "This class not yet implemented:\n"
            f"dto = {task_dto}\n"
            f"subjects len = {len(subjects)}\n"
            f"subjects = {subjects[:10]}"
        )
